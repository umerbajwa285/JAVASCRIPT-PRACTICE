//used for repeatative tasks
var table=Number(prompt("table user want to get"));

for (let i = 1; i < 10; i++) {
    if (i==6){
        break;
    }
    {console.log(table+" * "+i+ " = " +(table*i));
}}